; FOB true-tick accumulator DETERMINISM GATE (task 197/198) — emitter leg.
; fob_baysix v1.24.0 on a FIXED window, REAL TICKS (Model=4). Run the SAME ex5
; TWICE on this exact config; the two fob_capture_*.csv must be byte-identical
; (md5 match). This replaces the old "tick == closed-bar" parity (void by design:
; the tick accumulator records finer intra-bar order than the closed-bar replay).
; Run: terminal64.exe /config:<abs path>   (JM terminal MUST be closed — [[brc_headless_tester_fires]])
[Tester]
Expert=fob_system\fob_baysix.ex5
Symbol=XAUUSD_dukas
Period=M1
Model=4
FromDate=2022.01.01
ToDate=2022.04.01
Deposit=10000
Currency=USD
Leverage=1:100
Optimization=0
ShutdownTerminal=1
Report=fob_ticks_determinism_report
ReplaceReport=1
[TesterInputs]
InpSwingWindow=3
InpMaxAge=0
InpVisualize=false
