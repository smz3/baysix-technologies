# Handover — July 9, 2026 Morning2

## State
- **Analysis session — no EA code written, nothing compiled, nothing deployed.** Two hypotheses tested on existing data; one falsified, one survives; two data bugs found.
- **The VR Fresh definition locked THIS MORNING (spec v1.0, strategy_log 105) is SUPERSEDED** — see strategy_log **106**. The legacy `z.vr_fresh` field is CORRECT and must NOT be renamed or deleted.
- **CF3-edge = "wall already cleared" hypothesis: FALSIFIED** — strategy_log **107**.
- **Two data-integrity bugs in `data/fob_payload/run_19/zones.parquet`** (task **261**, P1): `mfe_r`/`mae_r` invalid; `confirm_time` future-conditioned.
- **Task 202 promoted to P1** — it is the single unblocker for tasks 260 / 240 / 245 / 262.
- **Task 260 downgraded to P2, BLOCKED** on 202 (and on the fact that the rule is a CF1 rule while we trade H4 CF3).
- Screen artifacts: [research/models/fob/room_r_screen.py](../research/models/fob/room_r_screen.py) + 3 CSV/JSON in [research/outputs/](../research/outputs/).

## Next
1. **(task 261, P1)** Quarantine the two bad columns: null `mfe_r`/`mae_r` on re-ingest, rename/drop `confirm_time`, add an assert in `ingest_fob`. Do this FIRST — 202 depends on trustworthy inputs.
2. **(task 202, P1)** Build true `mfe_r`/`mae_r`: signed forward excursion in R from Arctic ticks, **anchored at the CF event `bar_time`** (never `confirm_time`). `R = |entry − SL|`, entry = CF zone `l1`, SL = `l2 ∓ 0.5·band`.
3. **(task 262, P2)** Re-run the blocked-CF1 / `room_R` screen on the valid excursion, controlling for R. Then MT5 arbiter.

## Blockers
- **Tasks 260, 240, 245, 262 are all blocked on task 202** (no valid excursion measure exists). Nothing else is blocked.

## Why
- **The VR Fresh spec written this morning contradicted the manual it cited.** Vision-read of images 7.5/7.6 this session: the freshness window runs **VR birth → CF entry**, and the primitive is a bar **CLOSE** inside `[L1,L2]` (*"shadow does not count"*). Spec v1.0 had touch-based (wick counts) with the clock starting at 1st CF birth.
- **Two failure modes of the superseded def:** (1) the T-touch ladder fires on wicks almost immediately after VR birth, so every VR reads "structured" by CF1; (2) after entry, `vrf`-death **is** the VR-touch TP event, so it routes nothing — circular.
- **The legacy `z.vr_fresh` ([fob_lifecycle.mqh:51/213/298](../mt5/Include/fob_system/fob_lifecycle.mqh#L298)) already implements the manual definition exactly.** The only missing piece is a `vrf_at_cf1` snapshot bool latched at `cfCount` 0→1. One field, one assignment. (strategy_log 106)
- **Why 260 is blocked, not just re-specced:** the manual's TP-at-VR rule is explicitly for **Entry 1** (*"1st entry MUST TP in the VR zone"*, img 7.5); CF2+ is the layer-and-ride cohort (img 7.6). **We trade H4 CF3.** The rule cannot be A/B'd on the frozen config.
- **The CF3 reframe was tested and killed.** Median `room_R` by cf_idx (H4, run_19, [fob_room_r_by_cfidx_2026_07_09.csv](../research/outputs/fob_room_r_by_cfidx_2026_07_09.csv)): −0.122 / −0.057 / −0.162 / −0.186 for CF1–CF4; % VR-ahead flat 35–47%. **CF3 has no more open road than CF1.** `cf_idx` and `room_R` are independent axes — which is *good*: `room_R` survives as a genuinely new signal rather than a restatement of what we already trade. (strategy_log 107)
- **Why task 202 outranks everything:** blocked-CF1, `room_R`, and fresh/structured all reduce to *"how far did the trade go before it turned, and where was the wall?"* That is one join **once a valid excursion exists**, and unanswerable without it.

## Ruled-Out
- **Spec v1.0 of the VR Fresh detector (touch-based, CF1-birth clock, born-structured, no per-CF re-arm) — SUPERSEDED, strategy_log 106.** Do not rebuild it. Do not rename `z.vr_fresh`. [docs/specs/2026-07-09_fob_vr_fresh_detector_spec.md](../docs/specs/2026-07-09_fob_vr_fresh_detector_spec.md) §1/§2/§3 are now wrong; §0 (edge thesis) and §4 (the A/B design) still stand.
- **"cf_idx is a proxy for room-to-wall" — FALSIFIED, strategy_log 107.** Do not re-surface. Evidence: [fob_room_r_by_cfidx_2026_07_09.csv](../research/outputs/fob_room_r_by_cfidx_2026_07_09.csv).
- **`confirm_time` / `confirm_price` as an entry anchor — BANNED.** Non-null iff a later CF exists in the same cycle (perfect separation: 1,624 null vs 1,937 non-null = exactly `cf_idx < max_cf`). Filtering on it is look-ahead survivorship. **The first version of this session's screen did exactly that and produced a beautiful, fake t+7.72 — discarded.** Always anchor on the CF event `bar_time`.
- **`mfe_r`/`mae_r` in the current payload — BANNED.** min −17,778, max +32,120, median −1.86. An MFE cannot be negative. `ingest_fob_phase2` declares Part B deferred, yet the column is populated. (task 261)
- **`realized_r` as an arbiter of any of this — rejected as an instrument.** It is a naive fixed-RR2.0 barrier sim: no session filter, no trail, both directions, full span. The whole population is negative (E[R] −0.31 → −0.61 across CF1–CF4, [fob_room_r_by_cfidx_2026_07_09.csv](../research/outputs/fob_room_r_by_cfidx_2026_07_09.csv)), which **cannot reproduce the live CF3 result we trade** (+$3.23/tr OOS, result_id 50). Usable as a rough baseline only, never as a verdict.
- **task 239 D1/W1 confluence — still set aside** (not killed), unchanged from the prior handover. Do not re-surface as "the fix".

## Live-Threads
- **Blocked-CF1 hypothesis SURVIVES and is the main live thread.** Holding R fixed by tercile, wall-beyond-2R beats wall-INSIDE-2R in price units in **all three** terciles: +1.255 vs +0.812 (R_small), +0.910 vs −0.324 (R_mid), +3.760 vs −4.583 (R_large) — [fob_room_r_wall_by_Rtercile_2026_07_09.csv](../research/outputs/fob_room_r_wall_by_Rtercile_2026_07_09.csv). Same sign every tercile. **But** the `beyond 2R` cells are thin (n=53, n=16) and the instrument is `realized_r` (see Ruled-Out). Re-test on task-202 excursion → task 262.
- **Two measurement traps to carry into 262:** (a) `corr(1/R, room_R) = +0.566` — the pooled room-bucket table looks spectacular and is mostly the R effect in disguise; **always control for R**. (b) `room_R > 0` for only **40%** of entries — for the other 60% the entry sits inside/past the VR band and the near-edge measure is wrong there; use the **far** edge when entry is inside the band.
- **Zone-width contradiction, unresolved (task 263).** Narrow zones crush wide ones under the fixed-2R sim — R_small E[R] +0.979 / E[px] +1.002 (t+9.9) vs R_large E[R] −0.537 / E[px] −4.187 (t−9.0), [fob_zonewidth_Rtercile_2026_07_09.csv](../research/outputs/fob_zonewidth_Rtercile_2026_07_09.csv). Checked for the denominator illusion ([[er_denominator_illusion]]) — **it is not one**, narrow wins on dollars too. Yet our own IS result says **wide** was good under the trailing exit (`H4_cf3_wideRange_net_usd_per_trade` = +2.73). Hypothesis: **the sign flip is the EXIT, not the zone.** Likely settles tasks 259 and 250 as a by-product. This was the loudest effect in the data and nobody was looking for it.
- **Doc landmines, not yet fixed.** [2026-06-27_fob_storyline_alignment_findings.md](../docs/specs/2026-06-27_fob_storyline_alignment_findings.md) has **no warning banner** but its §2 "REAL hit-rate edge" is void (BRC-contaminated `tester_zones` run_id 5; [[fob_storyline_alignment_finding]] records it as resolved-as-artifact). [v0.2 entry-logic spec §6](../docs/specs/2026-07-02_fob_sequence_storyline_entry_logic_v0.2.md) points at retired `fob_trader.mq5`. Dissection numbers the "Sequence Counter" figure 2.1.5; the image itself says 2.1.6.
- **$50 rapid-scalp mandate still unscoped** (carried from prior handover). Manual's own taxonomy: *Scalp = M30 and below · Intraday = M30 and above · Swing = H4 and above* — so our live H4-CF3 config **is a swing setup by definition**, and the scalp mandate needs a different setup TF, not a different exit on the same trades. Min-lot DD floor at $50 is structural ([[orb_dd_structural_floor]]). Manual offers one concrete gold-specific, falsifiable scalp claim: the reversal "cheat code" (sideways at barrier + opposite BO failing to break it), *"M30 is the best for the reversal barrier"* / *"Use M30 for GOLD"* (imgs 6.5/6.6) — unclaimed, cheap to test.
- **Which-VR TP target (task 240) partially answered by the manual, not yet coded.** Conti trade → TP at the **cycle's own VR zone** (img 7.5). Counter/fade off a rejected VR → TP at the **setup-TF's BO zone** (img 4.6: *"You MUST TP in Daily zone"*). So the target depends on trade type, not on a leaf-vs-parent rule.
